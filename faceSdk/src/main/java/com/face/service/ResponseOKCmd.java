package com.face.service;

import com.face.model.VcardMessage;

public class ResponseOKCmd {
    public ResponseOKCmd(int deviceId, byte categoryCode, byte commandCode, byte data) {
        // vcardMessage = new VcardMessage(true, deviceId, );
        // VcardMessage(boolean bClientData, int deviceId, short infoCode, byte category, byte command, byte[] data)
    }

    private VcardMessage vcardMessage;
}
